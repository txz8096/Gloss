# Project dashboard example

This is a minimal Gloss handoff. Copy `design-dna.md` and `tokens.json` into a target project's `gloss/` directory, then ask Codex:

```text
Use $gloss-apply to create an empty state for the team task dashboard.
Read gloss/design-dna.md and gloss/tokens.json. Preserve the existing task flow.
```
